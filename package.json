{
  "name": "paperclip-hermes-railway",
  "version": "1.0.0",
  "description": "Railway wrapper for Paperclip + Hermes Agent with web-based setup",
  "private": true,
  "scripts": {
    "start": "node scripts/start.mjs",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "paperclipai": "latest",
    "@anthropic-ai/claude-code": "latest",
    "@openai/codex": "latest"
  },
  "engines": {
    "node": ">=20"
  }
}
